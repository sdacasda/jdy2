# Third-party notices

本项目在构建时从 `SOURCES.lock.json` 中列出的仓库获取固定提交，包括 LiBwrt、openwrt-daede、vmlinux-btf、Athena LED、Go feed、Argon、Argon Config 与 geodata 包。各组件遵循其上游许可证；本仓库不重新授权第三方源码。

完整仓库 URL 与提交 SHA 以 `SOURCES.lock.json` 为准。
